Place ici les librairies pour éviter les CDN (recommandé).
- libs/pdfjs/pdf.min.js
- libs/pdfjs/pdf.worker.min.js
- libs/pdf-lib/pdf-lib.min.js

Si les fichiers ne sont pas présents, l'app bascule automatiquement sur les CDN cdnjs (fallback).
